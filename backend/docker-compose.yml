version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fueleu
    ports:
      - "5432:5432"
    volumes:
      - fuel_eu_pgdata:/var/lib/postgresql/data

volumes:
  fuel_eu_pgdata: