FROM postgres:15-alpine

# Set default environment variables (matching your .env)
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=password
ENV POSTGRES_DB=fueleu

# Expose the default PostgreSQL port
EXPOSE 5432
